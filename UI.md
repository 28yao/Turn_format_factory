## 一、整体布局结构（字符版）





```
┌───────────────────────────────────────────────────────────────────────────┐
│ [PDF图标] PDF工具                                      [用户图标] 个人中心 ▼ │
├──────────┬────────────────────────────────────────────────────────────────┤
│ 导航栏   │ 主内容区（卡片式布局）                                           │
│ ┌──────┐ │ ┌───────────────────────────────────────────────────────────┐  │
│ │ 首页 │ │ │ 标题：PDF工具                                              │  │
│ ├──────┤ │ │ 副标题：高效、便捷的PDF处理工具，满足您的日常需求           │  │
│ │PDF转文件│ │ │ [右侧PDF图标占位]                                      │  │
│ ├──────┤ │ ├───────────────────────────────────────────────────────────┤  │
│ │文件转PDF│ │ │ 模块1：PDF转文件（5个卡片横向排列）                    │  │
│ ├──────┤ │ │ ┌──────┐┌──────┐┌──────┐┌──────┐┌──────┐                  │  │
│ │PDF编辑 │ │ │ │PDF转Word││PDF转Excel││PDF转PPT││PDF转图片││PDF转TXT│ │  │
│ ├──────┤ │ │ └──────┘└──────┘└──────┘└──────┘└──────┘                  │  │
│ │PDF处理 │ │ ├───────────────────────────────────────────────────────┤  │
│ └──────┘ │ │ 模块2：文件转PDF（5个卡片横向排列）                      │  │
│          │ │ ┌──────┐┌──────┐┌──────┐┌──────┐┌──────┐                  │  │
│          │ │ │Word转PDF││Excel转PDF││PPT转PDF││图片转PDF││TXT转PDF│   │  │
│          │ │ └──────┘└──────┘└──────┘└──────┘└──────┘                  │  │
│          │ │ ├───────────────────────────────────────────────────────┤  │
│          │ │ 模块3：PDF编辑（5个卡片横向排列）                        │  │
│          │ │ ┌──────┐┌──────┐┌──────┐┌──────┐┌──────┐                  │  │
│          │ │ │PDF编辑 ││PDF合并 ││PDF拆分 ││PDF压缩 ││PDF加密 │       │  │
│          │ │ └──────┘└──────┘└──────┘└──────┘└──────┘                  │  │
│          │ │ ├───────────────────────────────────────────────────────┤  │
│          │ │ 模块4：PDF处理（2个卡片横向排列）                        │  │
│          │ │ ┌──────┐┌──────┐                                          │  │
│          │ │ │PDF解密 ││PDF旋转 │                                      │  │
│          │ │ └──────┘└──────┘                                          │  │
│          │ └───────────────────────────────────────────────────────────┘  │
└──────────┴────────────────────────────────────────────────────────────────┘
```

------

## 二、字符限制与间距规则（AI 可直接使用）

### 1. 顶部导航栏

表格







| 元素         | 位置 | 字符限制                           | 间距                    |
| ------------ | ---- | ---------------------------------- | ----------------------- |
| 左侧 Logo 区 | 最左 | `[图标] PDF工具` 共≤10 字符        | 与左侧边框间距 2 字符   |
| 右侧用户区   | 最右 | `[用户图标] 个人中心 ▼` 共≤12 字符 | 与右侧边框间距 2 字符   |
| 背景         | 全宽 | 顶部栏高度固定，不换行             | 上下内边距各 1 字符高度 |

------

### 2. 左侧导航栏（侧边栏）

表格







| 元素        | 位置                  | 字符限制                       | 间距                              |
| ----------- | --------------------- | ------------------------------ | --------------------------------- |
| 导航项标题  | 左对齐                | 每项≤8 字符（如 “PDF 转文件”） | 每项上下间距 1 字符高度           |
| 图标 + 文字 | 图标在前，文字在后    | 图标 + 文字共≤10 字符          | 图标与文字间距 1 字符             |
| 选中态      | 高亮背景（如 “首页”） | 文字≤4 字符                    | 与侧边栏左右边框各留 2 字符内边距 |
| 侧边栏宽度  | 固定                  | 总宽度 = 16 字符               | 与主内容区间距 2 字符             |

------

### 3. 主内容区标题部分

表格







| 元素         | 位置       | 字符限制                                               | 间距                                           |
| ------------ | ---------- | ------------------------------------------------------ | ---------------------------------------------- |
| 主标题       | 左对齐     | `PDF工具` 共 4 字符                                    | 与顶部栏间距 3 字符高度，与左侧边框间距 2 字符 |
| 副标题       | 主标题下方 | `高效、便捷的PDF处理工具，满足您的日常需求` 共≤30 字符 | 与主标题间距 1 字符高度，不换行                |
| 右侧装饰图标 | 标题右侧   | 无文字，仅占位                                         | 与副标题右侧对齐，占 1/4 主内容区宽度          |

------

### 4. 功能模块通用规则（所有卡片模块）

表格







| 元素            | 位置                      | 字符限制                                            | 间距                                             |
| --------------- | ------------------------- | --------------------------------------------------- | ------------------------------------------------ |
| 模块标题        | 左对齐（如 “PDF 转文件”） | ≤8 字符                                             | 与上一模块间距 3 字符高度，与左侧边框间距 2 字符 |
| 卡片容器        | 模块标题下方              | 每行最多 5 个卡片，自动换行                         | 模块标题与卡片间距 2 字符高度                    |
| 单个卡片        | 横向排列                  | 宽度 = 16 字符，高度 = 8 字符                       | 卡片之间水平间距 2 字符，垂直间距 3 字符         |
| 卡片图标 + 文字 | 居中                      | 图标在前，文字在后，文字≤8 字符（如 “PDF 转 Word”） | 图标与文字间距 1 字符，上下居中                  |
| 卡片描述文字    | 卡片文字下方              | 单行≤20 字符，可换行                                | 与上方文字间距 1 字符高度，居中对齐              |

------

### 5. 各模块卡片明细（字符数 + 排列）

#### 模块 1：PDF 转文件（5 张卡片，1 行排列）

表格







| 卡片内容     | 主文字字符数 | 描述文字字符数                      |
| ------------ | ------------ | ----------------------------------- |
| PDF 转 Word  | 6            | `将PDF文件转换为Word文档`（≤18）    |
| PDF 转 Excel | 7            | `将PDF文件转换为Excel表格`（≤18）   |
| PDF 转 PPT   | 5            | `将PDF文件转换为PPT演示文稿`（≤20） |
| PDF 转图片   | 6            | `将PDF文件转换为图片格式`（≤16）    |
| PDF 转 TXT   | 6            | `将PDF文件转换为TXT文本`（≤16）     |

#### 模块 2：文件转 PDF（5 张卡片，1 行排列）

表格







| 卡片内容     | 主文字字符数 | 描述文字字符数                      |
| ------------ | ------------ | ----------------------------------- |
| Word 转 PDF  | 6            | `将Word文档转换为PDF文件`（≤16）    |
| Excel 转 PDF | 7            | `将Excel表格转换为PDF文件`（≤18）   |
| PPT 转 PDF   | 5            | `将PPT演示文稿转换为PDF文件`（≤20） |
| 图片转 PDF   | 6            | `将图片转换为PDF文件`（≤14）        |
| TXT 转 PDF   | 5            | `将TXT文本转换为PDF文件`（≤16）     |

#### 模块 3：PDF 编辑（5 张卡片，1 行排列）

表格







| 卡片内容 | 主文字字符数 | 描述文字字符数                   |
| -------- | ------------ | -------------------------------- |
| PDF 编辑 | 4            | `编辑PDF文件内容`（≤12）         |
| PDF 合并 | 4            | `将多个PDF文件合并为一个`（≤18） |
| PDF 拆分 | 4            | `将PDF文件拆分为多个文件`（≤18） |
| PDF 压缩 | 4            | `减小PDF文件大小`（≤12）         |
| PDF 加密 | 4            | `为PDF文件添加密码保护`（≤18）   |

#### 模块 4：PDF 处理（2 张卡片，1 行排列，靠左对齐）

表格







| 卡片内容 | 主文字字符数 | 描述文字字符数                 |
| -------- | ------------ | ------------------------------ |
| PDF 解密 | 4            | `解除PDF文件的密码保护`（≤18） |
| PDF 旋转 | 4            | `旋转PDF页面方向`（≤12）       |

------

### 6. 整体间距与对齐规则

1. **所有文字默认左对齐**，除了卡片内文字、副标题为居中 / 左对齐。

2. 水平间距

   ：

   - 侧边栏与主内容区：2 字符
   - 卡片之间：2 字符
   - 模块内边距：左右各 2 字符

   

3. 垂直间距

   ：

   - 顶部栏与主内容区：3 字符高度
   - 模块标题与卡片：2 字符高度
   - 卡片行之间：3 字符高度
   - 模块之间：3 字符高度

   

4. **换行规则**：所有描述文字单行放不下时，在卡片内自动换行，不超出卡片宽度