# 格式工厂小工具

## 领域词汇

| 术语 | 定义 |
|------|------|
| 源格式 (SourceFormat) | 输入图片的原始格式，由文件扩展名自动识别 |
| 目标格式 (TargetFormat) | 用户选择的输出格式，转换后的图片将保存为该格式 |
| 质量 (Quality) | 输出图片的压缩质量参数，仅对有损格式生效（JPG/WebP等），范围 1-100 |
| 尺寸缩放 (Resize) | 调整输出图片的宽高尺寸，支持按像素或百分比设定 |
| 保持宽高比 (AspectRatio) | 缩放时锁定原始宽高比，防止图片变形 |
| 单张模式 (SingleMode) | 一次选择一张图片进行转换，可精细调整参数 |
| 批量模式 (BatchMode) | 一次选择多张图片，统一转换到目标格式 |
| 输出目录 (OutputDir) | 转换后文件的保存位置，默认保存在源文件所在目录 |

## 支持的格式映射

| 格式 | 支持输入 | 支持输出 | 备注 |
|------|---------|---------|------|
| JPEG (.jpg/.jpeg) | 是 | 是 | 支持质量参数调节 |
| PNG (.png) | 是 | 是 | 无损格式 |
| WebP (.webp) | 是 | 是 | 支持质量参数调节 |
| BMP (.bmp) | 是 | 是 | 无压缩 |
| GIF (.gif) | 是 | 是 | 仅保留第一帧 |
| SVG (.svg) | 是 | 否 | 输入时栅格化，不可输出矢量 |
| ICO (.ico) | 是 | 是 | 固定尺寸输出 |
| TIFF (.tiff/.tif) | 是 | 是 | 高质量 |
| AVIF (.avif) | 是 | 是 | 支持质量参数调节 |
| HEIC (.heic) | 有限 | 否 | 输入依赖系统解码 |

## 已确定的设计决策

- **技术方案**: Electron 桌面应用，Sharp 做图片处理引擎
- **支持格式**: JPG/PNG/WebP/BMP/GIF/SVG/ICO/TIFF/AVIF/HEIC（10种输入，9种输出）
- **转换模式**: 单张模式 + 批量模式
- **附加功能**: 输出质量调节 + 尺寸缩放 + 保持宽高比选项
- **运行环境**: Windows 10及以上，Node.js v24.14.0
- **项目位置**: `d:\31689\Documents\code\ai coding\format-factory\`
- **打包方式**: electron-builder 打包为 Windows 安装程序
